#include <stdio.h>
#include "Board.h"
#include "BoardRenderer.h"
#include "BoardModel.h"
#include "InputModel.h"

int main() {
	int running = 1;

	// Initilize Board

	// Place default values

	// Gameloop
	while (running) {

		// Handle inputs with the InputModel

		// if left click and cell is not filled with default value
		// Select the cell
	
		// if 1-9 pressed and a cell is selected
		// Change the selected cell value
		// Check victory

		// If victory then 
		//display victory screen with the BoardRenderer
		// Else
		// Render board with BoardRenderer
	}

	return 0;
}